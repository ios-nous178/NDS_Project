---
"@nudge-design/mcp": patch
---

가이드 공백 해소 — FormSection · SelectionButton MCP 컴포넌트 가이드 추가(둘 다 figmaNodeUrl 보유). 외부 목업 프로젝트가 `get_guide({ topic: 'component:FormSection' })` / `component:SelectionButton` 으로 사용 규칙·함정을 받을 수 있다.
