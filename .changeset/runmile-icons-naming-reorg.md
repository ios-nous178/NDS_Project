---
"@nudge-design/icons": minor
---

런마일 아이콘 — Figma 런마일 Library 아이콘 가이드(5016:43) 반영 (레포 명명 SSOT 기준)

- **이름 정합**: `runmile-questionmark` → `runmile-question-mark` (+`-active`). 복합어 하이픈을 다른 아이콘(`check-circle`·`warning-circle`·`kebab-horizontal`)과 맞춤. ⚠ export 이름 변경 — `RunmileQuestionmarkIcon` → `RunmileQuestionMarkIcon`, `RunmileQuestionmarkActiveIcon` → `RunmileQuestionMarkActiveIcon`.
- **누락 아이콘 추가**: `runmile-location`(`RunmileLocationIcon`, 위치 핀) · `runmile-chat-color`(`RunmileChatColorIcon`, 멀티컬러 말풍선 — 가이드 Color 카테고리 완성).
- **아이콘 네이밍 가이드 문서화**(`pattern:iconography`): Figma 5016:43 의 패턴·어순·화살표≠셰브론·철자 규칙을 MCP 가이드에 반영. 단, **채움 `-fill`/상태 `-active/-inactive` 통일 제안은 레포 명명 SSOT(`-solid` + 축 분리)가 governing** 이라 미적용(가이드는 divergence 명시). 프로젝트 UI 아이콘 "범용화(prefix 제거)"도 다른 프로젝트 공용 아이콘을 덮어써 미적용.
