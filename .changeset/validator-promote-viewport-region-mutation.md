---
"@nudge-design/mcp": patch
---

목업 검증이 더 똑똑해졌어요 — 모바일 깨짐·잘못된 선택칩·빈 컴포넌트를 만드는 3가지 실수를 이제 **경고가 아니라 차단**으로 잡아드려요.

- 모바일에서 화면이 짓눌리는 viewport 누락
- 선택 결과를 잘못된 칩으로 표시(제거·개수 안 보임)
- nds 컴포넌트 내용을 직접 덮어써서 빈 박스가 되는 코드

세 가지 모두 자주 재발하던 실수라, 시안을 만들 때 처음부터 막아 더 안전하게 도와드립니다.
