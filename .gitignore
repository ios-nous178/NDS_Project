node_modules
dist
dist-mcpb
build
# 데스크탑 앱 아이콘은 build/ 안에 있어도 패키징(CI)에 필요하므로 추적.
!apps/desktop/build/
.turbo
.docusaurus
*.tsbuildinfo
storybook-static
coverage
chromatic-diagnostics.json
chromatic.log
.DS_Store
*.tgz
.ds-usage-log.jsonl
.ds-usage-webhook-queue.jsonl
.ds-context-log.jsonl
.claude/scheduled_tasks.lock

# MCP transcript 피드백 캡처 커서(로컬 관측 아티팩트)
.ds-feedback-cursor.json
