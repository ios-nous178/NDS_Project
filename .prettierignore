node_modules
dist
build
.turbo
.docusaurus
storybook-static
coverage
pnpm-lock.yaml

# 슬랙 mrkdwn 호환 본문 — prettier 가 `*Bold*` 를 `_Italic_` 으로 자동 변환하면
# 슬랙에서 bold 가 아니라 italic 으로 렌더되어 의도가 깨진다.
.release-notes/pending.md

# 자동 생성 SSOT 산출물 — 생성기(emit-manifest / generate-component-guides /
# generate-project-coverage)가 JSON.stringify(…, null, 2) 로 찍는 형식이 SSOT 다.
# prettier 가 짧은 배열을 한 줄로 접으면 생성기의 raw-text --check 와 어긋나
# lint:ssot(generate-*.mjs --check)가 stale 로 깨진다. (회귀: 커밋 시 lint-staged
# prettier 가 catalog/componentGuides 를 mangle 해 CI lint 가 빨개짐.)
packages/mcp/catalog.json
metadata/componentGuides.json
metadata/coverage-manifest.json

# generate-guide-docs.mjs 가 찍는 가이드 문서 — 생성기의 compact 테이블(`| a | b |`)이
# SSOT 다. prettier 가 테이블 파이프를 정렬·패딩하면 generate-guide-docs --check 가
# stale 로 깨진다(lint:ssot). 이 3개는 `<!-- AUTO-GENERATED -->` 헤더가 있는 생성물.
docs/guide/ux-writing.md
docs/guide/dark-patterns.md
docs/guide/visual-antipatterns.md

# validator-rule-report.mjs 가 찍는 검증 룰 리포트 — 위와 동일(compact 테이블 SSOT).
docs/validator-rule-report.md

# 가이드 본문 SSOT(.md) + 생성물 — build-guides.mjs 의 직렬화 포맷이 SSOT 다.
# prettier 가 리스트/펜스/frontmatter 를 손대면 build-guides --check(lint:ssot)와
# 무손실 왕복이 깨진다.
packages/mcp/guides-src/
packages/mcp/src/guides.generated.ts
# asset-catalog 생성기(build-asset-catalog.mjs)도 JSON.stringify(…, null, 2) 가 SSOT.
# prettier 가 한글 태그 배열을 한 줄로 접으면 build:assets --check 가 stale 로 깨진다.
packages/mcp/src/asset-catalog.generated.ts

# component-attrs 코드젠 산출물 — generate-component-attrs.mjs 의 직렬화(JSON.stringify 2-space)
# 가 SSOT. prettier 가 배열을 1줄로 접으면 generate-component-attrs --check(lint:ssot)와
# 충돌해 스테이징 시 커밋이 막힌다.
packages/html/src/generated/
