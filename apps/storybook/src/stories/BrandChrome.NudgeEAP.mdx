import { Meta, Canvas } from "@storybook/addon-docs/blocks";

import { BrandScope } from "../BrandScope";
import * as Banner from "./Banner.NudgeEAP.stories";
import * as Header from "./Header.NudgeEAP.stories";
import * as Footer from "./Footer.NudgeEAP.stories";
import * as BottomNav from "./BottomNav.NudgeEAP.stories";
import * as Icons from "./Icons.stories";

<Meta title="Brands/NudgeEAP/개요" />

# NudgeEAP

브랜드 전용 완성형 헤더/푸터/바텀네비를 한 곳에 모은 문서입니다.
실제 렌더는 Components 쪽 스토리와 같은 구현을 재사용합니다.

## Header

<BrandScope brand="nudge-eap">
  <Canvas of={Header.NudgeEAPWebHeaderDesktop} />
  <Canvas of={Header.NudgeEAPMobile} />
  <Canvas of={Header.NudgeEAPWebview} />
</BrandScope>

## Banner

<BrandScope brand="nudge-eap">
  <Canvas of={Banner.EAPIntro} />
  <Canvas of={Banner.TestRecommend} />
  <Canvas of={Banner.AppDownload} />
</BrandScope>

## Footer

<BrandScope brand="nudge-eap">
  <Canvas of={Footer.NudgeEAPWeb} />
  <Canvas of={Footer.NudgeEAPApp} />
</BrandScope>

## BottomNav

<BrandScope brand="nudge-eap">
  <Canvas of={BottomNav.NudgeEAPTabBar} />
</BrandScope>

## Icons

NudgeEAP 는 prefix 없는 base 아이콘이 브랜드 기본 셋입니다.
공용 아이콘은 `Foundations/Icons` 페이지를 그대로 쓰고, 브랜드 관점의 확인용으로만 아래 갤러리를 둡니다.

<Canvas of={Icons.All} />
