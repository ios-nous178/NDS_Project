---
title: AppointmentCard
description: 잡힌 상담 예약 카드. 날짜·시간·상담 방식·상태·액션 표시.
---

# AppointmentCard

이미 잡힌 상담 일정 한 건을 보여주는 카드입니다. 좌측에 날짜 블록, 우측에 제목·시간·방식·상태·액션이 배치됩니다.

> `CounselorCard`와 분리: 그건 상담사를 **선택하기 위한** 프로필 카드, 이건 **잡힌 일정**을 보여주는 카드.

## 사용 가이드

### 이럴 때 사용하세요
- "내 예약" 리스트
- 홈 화면 다음 일정 알림
- 상담사 페이지의 "내 다음 세션"

### 자주 하는 실수
- 상담사 선택 화면에 쓰지 말 것 — 그건 `CounselorCard`
- onClick(카드 전체)과 actions를 동시에 두면 액션 클릭 시 카드 onClick이 함께 발동되지 않음 (내부에서 stopPropagation 처리됨 — 의도적)

## 사용법

```tsx
import { AppointmentCard } from "@nudge-design/react";

<AppointmentCard
  date="2026-05-15"
  startTime="14:00"
  endTime="14:50"
  title="김민지 상담사 — 화상 상담"
  mode="video"
  status="confirmed"
  actions={[
    { label: "상세", onClick: openDetail },
    { label: "참여", onClick: joinSession, primary: true },
  ]}
/>

// 방문 상담 (장소 포함)
<AppointmentCard
  date="..."
  startTime="11:00"
  title="박지원 상담사"
  mode="in-person"
  location="강남센터 3층 301호"
  status="confirmed"
/>
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `date` *(필수)* | `string` | — | ISO 날짜 (YYYY-MM-DD) |
| `startTime` *(필수)* | `string` | — | 시작 시간 (HH:mm) |
| `endTime` | `string` | — | 종료 시간 (HH:mm, 선택) |
| `title` *(필수)* | `ReactNode` | — | 상담사/세션 제목 |
| `mode` | `AppointmentMode` | — | 상담 방식 |
| `location` | `ReactNode` | — | 장소 (in-person 모드일 때) 또는 보조 정보 |
| `status` | `AppointmentStatus` | `"scheduled"` | 상태 |
| `actions` | `AppointmentAction[]` | — | 카드 우측 하단 액션 |
| `onClick` | `() => void` | — | 카드 자체 클릭 (디테일 진입) |

<!-- AUTO-GEN:props:END -->

### `AppointmentAction`

| Prop      | 타입         | 설명                  |
| --------- | ------------ | --------------------- |
| `label`   | `string`     | 버튼 라벨             |
| `onClick` | `() => void` | 클릭 콜백             |
| `primary` | `boolean`    | 강조 (1차 액션 스타일) |
