---
title: AudioPlayer
description: 명상·이완 가이드 오디오 플레이어. UI 전용, 상태는 외부 관리.
---

# AudioPlayer

명상·이완 가이드 오디오 플레이어입니다. **UI만 제공**하고 재생 상태(`playing`/`currentTime`/`duration`)는 외부에서 관리합니다.

## 사용법

```tsx
import { AudioPlayer } from "@nudge-design/react";

const audioRef = useRef<HTMLAudioElement>(null);
const [playing, setPlaying] = useState(false);
const [time, setTime] = useState(0);

<AudioPlayer
  title="마음을 편안하게 하는 호흡 명상"
  subtitle="10분 가이드 · 박지수"
  playing={playing}
  onPlayPause={(next) => {
    if (next) audioRef.current?.play();
    else audioRef.current?.pause();
    setPlaying(next);
  }}
  currentTime={time}
  duration={600}
  onSeek={(t) => {
    if (audioRef.current) audioRef.current.currentTime = t;
    setTime(t);
  }}
/>
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `title` | `ReactNode` | — | 제목 (예: "마음을 편안하게 하는 명상") |
| `subtitle` | `ReactNode` | — | 부제 (예: "10분 가이드") |
| `playing` *(필수)* | `boolean` | — | 재생 중 여부 |
| `onPlayPause` *(필수)* | `(next: boolean) => void` | — | 재생/일시정지 토글 |
| `currentTime` *(필수)* | `number` | — | 현재 재생 위치 (초) |
| `duration` *(필수)* | `number` | — | 총 길이 (초) |
| `onSeek` | `(time: number) => void` | — | 시크 콜백 (초 단위) |
| `onSkipBack` | `() => void` | — | 이전 트랙 (제공 시 표시) |
| `onSkipForward` | `() => void` | — | 다음 트랙 (제공 시 표시) |

<!-- AUTO-GEN:props:END -->

## 가이드

- 재생 상태는 `useState` + `<audio>` ref로 부모에서 관리. DS는 UI만 그림.
- `<audio>`의 `timeupdate` 이벤트로 `currentTime` 동기화 권장.
- 단일 트랙은 `onSkipBack/Forward` 생략, 시리즈는 둘 다 부착.
