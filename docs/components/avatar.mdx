---
title: Avatar
description: 사용자 프로필 이미지 컴포넌트. 이미지, 이니셜, 기본 아이콘 폴백을 지원합니다.
---

# Avatar

사용자 프로필 이미지 컴포넌트입니다. 이미지 로드 실패 시 이니셜 또는 기본 아이콘으로 폴백됩니다.

## 사용법

```tsx
import { Avatar } from "@nudge-design/react";

// 이미지
<Avatar src="/profile.jpg" alt="홍길동" size="lg" />

// 이니셜 폴백 (이미지 없을 때)
<Avatar name="홍길동" size="md" />

// 기본 아이콘 폴백
<Avatar size="sm" />

// 커스텀 폴백
<Avatar fallback={<CustomIcon />} />
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `src` | `string \| null` | — | 이미지 URL |
| `alt` | `string` | `""` | 이미지 alt 텍스트 |
| `name` | `string` | — | 폴백에 표시할 이름 (이니셜 추출) |
| `size` | `AvatarSize` | `"md"` | 크기 (Figma 24/32/48/64/96 = xs/sm/md/lg/xl) |
| `shape` | `AvatarShape` | `"circle"` | 모양 (Figma Shape): circle(인물 프로필·기본) · rounded(앱/썸네일) · square(콘텐츠/제품 이미지) |
| `fallback` | `ReactNode` | — | 커스텀 폴백 렌더 |

<!-- AUTO-GEN:props:END -->
