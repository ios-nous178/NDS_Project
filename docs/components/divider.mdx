---
title: Divider
description: 콘텐츠 구분선 컴포넌트. 수평/수직 방향을 지원합니다.
---

# Divider

콘텐츠 영역을 시각적으로 분리하는 구분선입니다.

## 사용법

```tsx
import { Divider } from "@nudge-design/react";

// 수평 구분선
<Divider />

// 간�� 포함
<Divider spacing={16} />

// 수직 구분선 (인라인 텍스트 사이)
<div style={{ display: "flex", alignItems: "center" }}>
  <span>항목 A</span>
  <Divider orientation="vertical" />
  <span>항목 B</span>
</div>

// 두꺼운 섹션 구분선
<Divider thickness={8} color="#f5f5f5" />
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `orientation` | `DividerOrientation` | `"horizontal"` | 방향 |
| `type` | `DividerType` | `"line"` | 분리 강도. - `line` (기본): 1px 라인 — 리스트/카드 내부 분할. - `block`: 8px 청크 — 섹션 사이 BG/Section 분할(가로 전용). |
| `tone` | `DividerTone` | `"normal"` | Border 강도 — subtle(약)/normal(기본)/strong(강). |
| `thickness` | `number` | — | 두께 (px) — escape hatch |
| `spacing` | `number` | — | 양쪽 간격 (px) |
| `color` | `string` | — | 색상 오버라이드 — escape hatch |

<!-- AUTO-GEN:props:END -->
