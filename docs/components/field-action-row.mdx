---
sidebar_position: 9
title: FieldActionRow
description: 입력 필드와 버튼을 한 줄로 조합하는 레이아웃 컴포넌트
category: 레이아웃
related: [Input, Button]
---

import { FieldActionRow, Button } from "@nudge-design/react";

# FieldActionRow

입력 필드 + 액션 버튼 조합 레이아웃 컴포넌트입니다. 휴대폰 인증, 인증번호 입력 등 "입력 → 확인" 패턴에 특화되어 있습니다.

```tsx
import { FieldActionRow } from "@nudge-design/react";
```

---

## 사용 가이드

### 이럴 때 사용하세요

- **입력 필드 + 버튼**을 한 줄로 배치할 때 (인증번호 발송, 쿠폰 적용, 타이머 등)
- Input과 Button의 높이/정렬을 자동으로 맞춰야 할 때
- 에러 상태, 타이머 등 부가 정보를 입력 줄 아래에 표시할 때

### 이럴 때 사용하지 마세요

- 버튼 없이 입력만 필요할 때 → [Input](/components/input)을 사용하세요
- 검색 기능 → [SearchInput](/components/search-input)을 사용하세요
- 복잡한 폼 레이아웃 (여러 필드 + 여러 버튼) → 개별 Input + Button 조합을 사용하세요

### 자주 하는 실수

- 복잡한 폼 전체를 FieldActionRow로 구성 — 이 컴포넌트는 "입력 1개 + 버튼 1개" 패턴에 최적화되어 있습니다

---

## 사용법

### 휴대폰 인증

```tsx
<FieldActionRow
  field={
    <input
      type="tel"
      value={phone}
      onChange={(e) => setPhone(e.target.value)}
      placeholder="숫자만 입력해 주세요."
    />
  }
  action={
    <button type="button" disabled={phone.length < 10} onClick={handleSend}>
      인증 번호 받기
    </button>
  }
  actionTone="outline"
/>
```

### 인증번호 입력 + 타이머

```tsx
<FieldActionRow
  field={<input type="number" placeholder="인증번호 6자리" />}
  action={<button type="button">확인</button>}
  actionTone="solid"
  timer="02:58"
  helperText="이메일 또는 문자로 전송된 인증번호를 입력해주세요"
/>
```

### 에러 상태

```tsx
<FieldActionRow
  field={<input type="tel" value="010-0000-0000" />}
  action={<button disabled>인증 번호 받기</button>}
  error
  helperText="휴대폰 번호를 확인해주세요."
/>
```

### 타이머 만료

```tsx
<FieldActionRow
  field={<input type="number" placeholder="인증번호 6자리" />}
  action={<button disabled>확인</button>}
  timer="00:00"
  timerExpired
  error
  helperText="인증 시간이 만료되었습니다."
/>
```

---

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `label` | `ReactNode` | — | 필드 라벨 (옵션). 주면 입력+버튼 한 줄 **위**에 라벨이 렌더된다 — 라벨이 필요한 인증 row (예: 캐포비 본인인증의 "휴대폰 번호" + [인증번호 받기])는 라벨을 손으로 한 줄에 욱여넣지 말고 이 prop 으로 넘긴다(라벨은 위, 입력+버튼은 한 줄 인라인으로 컴포넌트가 정렬한다). |
| `field` *(필수)* | `ReactNode` | — | 입력 필드 (Input 또는 native input) |
| `action` | `ReactNode` | — | 액션 버튼 (옵션). 생략하면 "필드(+타이머)만" 한 줄 — 인라인 버튼 없이 코드 입력 + 우측 타이머만 두는 레이아웃(예: 캐포비 본인인증의 별도 full-width 재전송 버튼 + 타이머만 있는 코드 입력). |
| `actionTone` | `"outline" \| "solid"` | `"outline"` | 버튼 톤 |
| `timer` | `ReactNode` | — | 타이머 표시 |
| `timerExpired` | `boolean` | `false` | 타이머 만료 상태 |
| `error` | `boolean` | `false` | 에러 상태 |
| `helperText` | `string` | — | 도움/에러/성공 텍스트 |
| `success` | `boolean` | `false` | 성공 상태 |
| `className` | `string` | — | 루트 className |
| `style` | `CSSProperties` | — | 루트 style |
| `slotProps` | `FieldActionRowSlotProps` | — | 슬롯 프롭 |

<!-- AUTO-GEN:props:END -->

---

## 상태별 스타일

| 상태        | 필드 테두리 | 헬퍼 텍스트 | 타이머  |
| ----------- | ----------- | ----------- | ------- |
| 기본        | Neutral 300 | Neutral 500 | Primary |
| 포커스      | Blue 500    | —           | —       |
| 에러        | Red 500     | Red 500     | —       |
| 성공        | —           | Green 300   | —       |
| 타이머 만료 | —           | —           | Red 500 |

---

## 마이그레이션

```tsx
// Before (SignUpPhoneInput)
<div className="flex gap-2">
  <input className="..." />
  <button className="...">인증 번호 받기</button>
</div>

// After
<FieldActionRow
  field={<input type="tel" placeholder="숫자만 입력해 주세요." />}
  action={<button>인증 번호 받기</button>}
  actionTone="outline"
/>
```

---

## data-slot 구조

```
div [data-slot="root"]
├── div [data-slot="row"]
│   ├── div [data-slot="field"] [data-error]
│   │   ├── input
│   │   └── span [data-slot="timer"] [data-expired]
│   └── div [data-slot="action"] [data-tone]
│       └── button
└── span [data-slot="helper"] [data-error] [data-success]
```
