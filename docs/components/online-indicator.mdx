---
title: OnlineIndicator
description: 온라인/자리비움/상담중/오프라인 점 인디케이터.
---

# OnlineIndicator

상담사/사용자의 온라인 상태를 점으로 표현합니다. online은 펄스 애니메이션이 자동 적용됩니다.

## 사용 가이드

### 이럴 때 사용하세요
- 상담사 리스트 옆 상태 표시
- 아바타 우하단 점
- 그룹 채팅 참가자 상태

### 자주 하는 실수
- "online"은 펄스 애니메이션이 자동 — 별도 강조 효과 추가 X
- 아바타 위에 올릴 때는 부모를 `position: relative`로 두고 점은 `position: absolute`

## 사용법

```tsx
import { OnlineIndicator } from "@nudge-design/react";

<OnlineIndicator status="online" showLabel />

// 라벨 없이 (아바타 위 등)
<OnlineIndicator status="busy" size={10} />

// 라벨 커스텀
<OnlineIndicator status="busy" showLabel label="상담 중" />
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `status` *(필수)* | `PresenceStatus` | — | 상태 |
| `showLabel` | `boolean` | `false` | 라벨 표시 (지정 시 텍스트도 옆에) |
| `label` | `string` | — | 라벨 오버라이드 |
| `size` | `number` | `8` | 점 크기 px (기본 8) |

<!-- AUTO-GEN:props:END -->
