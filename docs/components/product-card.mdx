---
title: ProductCard
description: 상품 카드. 썸네일 + 제목 + 가격 + 뱃지/품절 처리.
---

# ProductCard

상품 그리드/리스트의 한 칸을 표현하는 카드입니다. 가격은 `PriceTag` 컴포넌트를 슬롯으로 받습니다.

## 사용 가이드

### 이럴 때 사용하세요
- 굿즈/콘텐츠 패키지 그리드
- 상품 검색 결과
- 추천 상품 캐러셀

### 자주 하는 실수
- 가격은 `price` 슬롯에 `<PriceTag />`를 직접 넘김 — string 가격 넣지 말 것 (포맷 일관성 깨짐)
- 품절은 `soldOut`로 — 외곽 뱃지(`badge`)와 다름 (overlay)

## 사용법

```tsx
import { ProductCard, PriceTag } from "@nudge-design/react";

<ProductCard
  thumbnail="/products/book.jpg"
  description="마음챙김 굿즈"
  title="마음챙김 가이드북"
  price={<PriceTag amount={29000} originalAmount={39000} />}
  footer={<>★ 4.8 (124)</>}
  badge="NEW"
  onClick={openDetail}
/>

// 품절
<ProductCard ... soldOut />
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `size` | `ProductCardSize` | `"sm"` | 사이즈 — `sm`: 140w(모바일), `md`: 236w(데스크탑). 기본 `sm` |
| `thumbnail` | `string` | — | 썸네일 이미지 URL |
| `thumbnailAlt` | `string` | `""` | 썸네일 alt |
| `badge` | `ReactNode` | — | 좌상단 텍스트 뱃지 (예: "NEW", "BEST"). `soldOut`/`rankingNumber` 와 동시 노출 금지 |
| `rankingNumber` | `number` | — | 좌상단 랭킹 배지 (1~N). 캐시딜 랭킹용. `badge` 보다 우선 — 동시 지정 시 ranking 만 노출 |
| `title` *(필수)* | `ReactNode` | — | 상품 제목 — 2줄 ellipsis |
| `originalPrice` | `number` | — | 원가 (취소선) — 할인 전 가격. `price` 보다 위에 노출 |
| `discountPercent` | `number` | — | 할인율 (예: 31 → "31%"). 0/undefined 면 숨김 |
| `price` *(필수)* | `number` | — | 가격 (KRW 단위 숫자). 자동으로 천단위 콤마 포맷 |
| `currency` | `ReactNode` | `"원"` | 가격 단위 (기본: "원") |
| `reward` | `ProductCardReward` | — | 적립 칩 — 캐시딜 포인트 아이콘 + 금액 + "적립" |
| `freeShipping` | `boolean` | — | 무료배송 칩 |
| `pointDiscount` | `boolean` | — | "포인트할인" 외곽선 칩 (모바일 캐시딜 패턴) |
| `buyersCount` | `number` | — | 구매자 수 — 자동 포맷. 10,000 이상은 "9,999+명" 으로 truncate |
| `rating` | `number` | — | 별점 (0~5) |
| `reviewCount` | `number` | — | 리뷰 수 — rating 우측 "(1,208)" 노출. rating 없으면 무시 |
| `soldOut` | `boolean` | `false` | 품절 표시 (썸네일 위 오버레이) |
| `onClick` | `() => void` | — | 카드 클릭 |

<!-- AUTO-GEN:props:END -->
