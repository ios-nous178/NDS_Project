---
title: Skeleton
description: 콘텐츠 로딩 중 표시하는 플레이스홀더 컴포넌트.
---

# Skeleton

콘텐츠가 로딩되는 동안 표시하는 펄스 애니메이션 플레이스홀더입니다.

## 사용법

```tsx
import { Skeleton } from "@nudge-design/react";

// 사각형 (카드, 이미지 영역)
<Skeleton width={200} height={120} />

// 원형 (아바타)
<Skeleton variant="circular" width={40} />

// 텍스트 라인
<Skeleton variant="text" width="80%" />
<Skeleton variant="text" width="60%" />
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `variant` | `SkeletonVariant` | `"rectangular"` | 형태 변형 |
| `width` | `number \| string` | — | 너비 (px 또는 CSS 값) |
| `height` | `number \| string` | — | 높이 (px 또는 CSS 값) |
| `radius` | `number` | — | 코너 반경 오버라이드 |

<!-- AUTO-GEN:props:END -->
