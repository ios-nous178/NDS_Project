---
title: Spinner
description: 인라인 회전 로딩 인디케이터. 짧은 fetch에 사용합니다.
---

# Spinner

인라인 회전 로딩 인디케이터입니다. 2초 미만의 짧은 비동기 작업에 사용하고, 더 긴 로딩은 `Skeleton`을 사용하세요.

## 사용법

```tsx
import { Spinner, Button } from "@nudge-design/react";

<Spinner />
<Spinner size="sm" />
<Spinner size={48} />

// 버튼 내부
<Button disabled>
  <Spinner size="sm" color="currentColor" />
  <span>처리 중...</span>
</Button>
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `size` | `SpinnerSize \| number` | `"md"` | 크기 |
| `color` | `string` | — | 색상 오버라이드 (CSS color 값 또는 토큰 var) |
| `label` | `string` | `"로딩 중"` | 접근성 라벨 |

<!-- AUTO-GEN:props:END -->

## 가이드

- 전체 페이지 로딩에 단독으로 띄우지 말 것 — 빈 화면 인상이 강합니다. 레이아웃을 유지하는 `Skeleton`이 UX에 더 좋음.
- 버튼 내부에서는 `color="currentColor"`로 컨텍스트 색에 맞춤.
