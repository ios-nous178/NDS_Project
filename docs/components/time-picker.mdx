---
title: TimePicker
description: 시간만 선택. HH:mm 입력, step/min/max 지원.
---

# TimePicker

날짜 없이 시간(HH:mm)만 선택하는 컴포넌트입니다. 알림 시각, 복약 시간 등 "하루 중 시각" 입력에 사용하세요.

## 사용 가이드

### 이럴 때 사용하세요
- 알림 / 리마인더 시각 설정
- 복약 시간
- 상담 슬롯 시작 시간

### 이럴 때 사용하지 마세요
- 날짜 + 시간 → `DatePicker` + `TimePicker` 조합 또는 별도 datetime

## 사용법

```tsx
import { TimePicker } from "@nudge-design/react";

<TimePicker
  label="알림 시각"
  value={time}
  onValueChange={setTime}
  step={60 * 5}
  min="08:00"
  max="22:00"
/>
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `value` *(필수)* | `string` | — | 값 (`HH:mm`) |
| `onValueChange` *(필수)* | `(value: string) => void` | — | 변경 콜백 |
| `step` | `number` | — | step 분 단위 (초 단위 — 60 = 1분, 300 = 5분). 분 컬럼 간격으로 환산된다. |
| `label` | `ReactNode` | — | 라벨 |
| `placeholder` | `string` | `"시간 선택"` | 미선택 시 트리거 placeholder |
| `helperText` | `ReactNode` | — | 헬퍼 / 에러 텍스트 |
| `error` | `boolean` | `false` | 에러 |
| `fullWidth` | `boolean` | `false` | 가로 가득 |
| `disabled` | `boolean` | `false` | 비활성화 |
| `min` | `string` | — | 선택 가능한 최소 시각 (HH:mm, 포함) |
| `max` | `string` | — | 선택 가능한 최대 시각 (HH:mm, 포함) |
| `portalContainer` | `HTMLElement \| null` | — | 포털 컨테이너 |
| `presets` | `TimePickerPreset[]` | — | 빠른설정 프리셋 칩 (예: `[{ label: "자정까지", value: "23:59" }]`). 클릭하면 value 를 즉시 세팅. 시간 필드 트레일링(시계아이콘 우측)에 붙는 회색 중립 보조 액션 칩으로 렌더 — 노란 project Chip / SelectionButton 이 아니다(캐포비 어드민 시간 인풋, Figma 3001:19122). |

<!-- AUTO-GEN:props:END -->
