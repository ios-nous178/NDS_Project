---
title: VoiceRecorder
description: 음성 메모 녹음 UI. 큰 녹음 버튼 + 타이머 + 펄스 인디케이터.
---

# VoiceRecorder

녹음 시작/정지 버튼과 진행 시간을 보여주는 UI 컴포넌트입니다. 실제 마이크 접근 / 파일 저장은 외부에서 처리하고, 이 컴포넌트는 **시각적 인디케이터**만 담당합니다.

## 사용 가이드

### 이럴 때 사용하세요
- 감정 일기 음성 모드
- 챗 음성 메시지
- 상담 자료 음성 메모

### 자주 하는 실수
- `seconds`는 외부 timer로 갱신해야 함. 컴포넌트는 시간 자체를 측정하지 않음 (테스트 가능성/정확도)
- `maxSeconds` 도달 시 자동으로 `onStateChange("idle")` + `onComplete(sec)` 호출

## 사용법

```tsx
import { VoiceRecorder, type VoiceRecorderState } from "@nudge-design/react";

const [state, setState] = useState<VoiceRecorderState>("idle");
const [seconds, setSeconds] = useState(0);

useEffect(() => {
  if (state !== "recording") return;
  const t = setInterval(() => setSeconds((s) => s + 1), 1000);
  return () => clearInterval(t);
}, [state]);

<VoiceRecorder
  state={state}
  onStateChange={(s) => {
    if (s === "idle") setSeconds(0);
    setState(s);
    // 외부에서 navigator.mediaDevices.getUserMedia 등 처리
  }}
  seconds={seconds}
  maxSeconds={60}
  onComplete={(sec) => saveRecording(sec)}
/>
```

## Props

<!-- AUTO-GEN:props:START -->
<!-- 이 표는 packages/react/src 의 Props 인터페이스에서 자동 생성됩니다. 직접 고치지 말고 소스의 타입/JSDoc 을 고친 뒤 `pnpm generate:component-docs` 를 실행하세요. -->

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `state` *(필수)* | `VoiceRecorderState` | — | 현재 상태 (controlled) |
| `onStateChange` *(필수)* | `(state: VoiceRecorderState) => void` | — | 상태 변경 콜백 |
| `seconds` *(필수)* | `number` | — | 진행 시간 (초) — 외부 timer로 갱신 |
| `maxSeconds` | `number` | — | 최대 녹음 시간 (초) |
| `onComplete` | `(seconds: number) => void` | — | 녹음 완료 시 (사용자 stop 직후) |
| `idleLabel` | `string` | `"버튼을 눌러 녹음을 시작하세요"` | 라벨 (idle 상태에서 표시) |
| `recordingLabel` | `string` | `"녹음 중"` | 녹음 중 라벨 |

<!-- AUTO-GEN:props:END -->
