/**
 * [shim] @nudge-design/mockup-core 로 추출된 모듈의 재export.
 * 구현은 core 에 있다. server.ts / usage.ts / 테스트가 기존 상대경로를 그대로
 * import 하도록 유지하기 위한 임시 파일 — Phase 0 step 4(정리)에서 제거 예정.
 */
export * from "@nudge-design/mockup-core/tools/usage/parser";
