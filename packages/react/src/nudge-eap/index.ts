/**
 * NudgeEAP 브랜드 자산 — Logo.
 *
 * 브랜드 chrome(헤더/푸터/바텀네비)은 공개 react 패키지에서 제거됐다(브랜드 분기 금지).
 * 화면 chrome 은 목업 전용 `nds-brand-chrome`(`<nds-brand-header brand="nudge-eap">` 등)으로,
 * 공개 primitive 가 필요하면 `Header` / `BottomNav` / `Footer` + 브랜드 토큰을 쓴다.
 */
export * from "./Logo.js";
