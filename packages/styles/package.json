{
  "name": "@nudge-design/styles",
  "version": "0.0.4",
  "description": "Framework-agnostic CSS bundle for Nudge Design System — consumed by @nudge-design/react and @nudge-design/html.",
  "exports": {
    "./styles.css": "./dist/styles.css"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && node scripts/extract-styles.mjs",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nudge-design/tokens": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
